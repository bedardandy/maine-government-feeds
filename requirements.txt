# Pinned exactly (not floored with >=) so every build — local or CI —
# resolves the same dependency tree. Bump deliberately and re-run
# build + validate after any change.
httpx==0.28.1
beautifulsoup4==4.15.0
lxml==6.1.2
PyYAML==6.0.3
feedgen==1.0.0
python-dateutil==2.9.0.post0
feedparser==6.0.14
anthropic==1.0.0
pypdf==6.16.1
